import React from 'react'

function UseEffectAPI({ title, imageURL }) {
    return (
        <div>
            <h2>Title: {title}</h2>
            <h3>ImageURL: {imageURL}</h3>
        </div>
    )
}

export default UseEffectAPI
